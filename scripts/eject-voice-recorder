#!/usr/bin/env bash
set -euo pipefail

mount_path="/Volumes/IC RECORDER"

if [[ ! -d "$mount_path" ]] || ! diskutil info "$mount_path" >/dev/null 2>&1; then
  echo "not attached"
  exit 0
fi

diskutil eject "$mount_path"
